from setuptools import find_packages, setup

setup(name='hrrpmaps',
      version='1.0',
      description='code for auto generating maps for HRRP Nepal',
      author='eoglethorpe',
      packages=['hrrpmaps'] + find_packages(),
      )
